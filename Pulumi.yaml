name: cloud-computing-exercise2
runtime:
  name: python
  options:
    virtualenv: venv
description: A Python program to deploy a serverless application on AWS for exercise1
config:
  pulumi:tags:
    value:
      pulumi:template: serverless-aws-python
